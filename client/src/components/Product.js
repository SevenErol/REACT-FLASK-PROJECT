import React, { useEffect, useState } from 'react'
import Card from 'react-bootstrap/Card';
import Button from 'react-bootstrap/Button';


const Product = ({ id, name, description, price, stock, category_id }) => {
    <h3>{name}</h3>

}

export default Product